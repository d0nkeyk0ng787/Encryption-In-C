#include <stdio.h>
#include <stdlib.h>
#include <time.h>

long RandomPrimes()
{
    return 0;
}

long RabinMiller()
{
    return 0;
}

char * Encryptor()
{
    long p;
    long q;
    long n;
    int e;

    return "test";
}

char * Decryptor()
{
    return "\ntest2";
}

int main()
{
    char * plainText;

    printf("Enter a string to be encrypted: ");
    scanf("%s", &plainText);

    // printf(Encryptor());
    // printf(Decryptor());
    return 0;
}